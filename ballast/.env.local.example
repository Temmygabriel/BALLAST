# Where the guardian's state feed lives.
# Leave unset for local dev (guardian sim server on :4300). For a deployed demo,
# set this to the guardian's public URL. Never starts with a trailing slash.
# NEXT_PUBLIC_GUARDIAN_URL=http://localhost:4300
